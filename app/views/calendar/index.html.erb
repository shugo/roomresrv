<div id="room-legend">
  <% @rooms.each do |room| %>
    <span class="room" style="background-color: <%= @room_colors[room.id] %>">
      <%= room.name_with_office %>
    </span>
  <% end %>
</div>
<div id="calendar"></div>
