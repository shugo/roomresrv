以下の予定が登録されました。

担当者: <%= @reservation.representative %>
目的: <%= @reservation.purpose %>
日時: <%= @reservation.start_at.strftime("%Y/%m/%d(%a) %H:%M") %> 〜 <%= @reservation.start_at.strftime("%Y/%m/%d(%a) %H:%M") %>
メモ:
<%= @reservation.note %>
